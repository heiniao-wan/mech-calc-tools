# 每日工作流程（与 AI 协作模板）

> 每天打开 WorkBuddy 时，只需说一句话即可开工，例如：
> **"继续机械计算工具集项目，今天做第 9 个：滚珠丝杠选型"** 或 **"今天做下一个计算工具"**

## 每日标准流程（单日一个工具）

1. **确定今日工具**：按 `ROADMAP.md` 顺序取下一个 ⬜ 项；也可临时插队（如项目急需某计算）。
2. **查资料**：先查 `SOURCES.md` 对应来源；来源缺失时全网搜索并补充进 SOURCES.md。
3. **编写工具**：
   - HTML 单文件放在 `tools/<英文id>.html`，复制 `tools/beam-calc.html` 的页面骨架（头部渐变样式、返回链接、面板结构）保持风格统一；
   - 每个公式在页面底部注明出处；
   - 输入参数带单位、带合理默认值；输出带校核判定（✔/✘）。
4. **注册到总页面**：在 `registry.js` 的 TOOLS 数组**顶部追加一条**（新工具显示在最前面），含 id/name/file/category/desc/icon/date。
5. **验证**：用典型例题验算 1~2 组数据（手算对照），确认结果正确。
6. **更新记录**：
   - `ROADMAP.md` 中对应项打 ✅ 并标日期；
   - 在 `~/.workbuddy/MEMORY.md` 的"进度"处更新已完成数量与最近日期。
7. **（可选）提交 Git & 部署**：仓库建好后每日 `git add -A && git commit -m "add: 工具名"`，GitHub Pages 自动发布。

## 目录结构
```
mech-calc-tools/
├── index.html        # 总页面（发布入口，GitHub Pages 首页，一般不用改）
├── registry.js       # 工具注册表（每天只改这里）
├── ROADMAP.md        # 开发路线图（打勾进度）
├── SOURCES.md        # 公式资料来源库
├── PLAN.md           # 本文件：每日流程
└── tools/
    ├── beam-calc.html
    └── ...           # 每个工具一个独立 HTML/Python 文件
```

## 规范约定
- **风格**：白色/淡色背景 + 橙红渐变头部，纯 HTML/CSS/JS 单文件，无外部 CDN 资源。
- **语言**：全中文界面；参数符号与《机械设计手册》保持一致。
- **单位**：输入输出全部显式标注单位；内部统一 N、mm、MPa。
- **安全**：所有结果仅作设计参考，页面必须带"以厂商样本及规范校核为准"提示。
- **Python 工具**：当算法复杂（如需查表迭代）时用 Python，输出控制台/生成报告，注册表 file 指向 .py。
